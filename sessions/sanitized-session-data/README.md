# Sanitized session data

No raw session dataset is included merely by creating this directory. Add only sanitized, redistributable JSON or JSONL files. Remove API keys, account identifiers, private URLs, and personal data while preserving model-generated content and measurement metadata.
