# Tokenizer fingerprint manifest

Place prompts, reference-model outputs, token counts, scoring code, and negative controls here. The reports state a 30/30 tokenizer match with the GLM-5.3 family. Reproduction requires exact probe definitions and raw counts.
