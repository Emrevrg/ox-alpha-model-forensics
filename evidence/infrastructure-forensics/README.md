# Evidence manifest

Place raw, sanitized infrastructure artifacts in this directory. Each artifact must document collection time, route, parameters, observed payload, redactions, and alternative explanations. The reports cite a Java namespace containing `com.wd.paas.api.domain.v4.chat.ChatCompletionRequest` and error-code behavior; publish the complete sanitized payload before treating it as independently auditable evidence.
