# Analysis scripts

Scripts added here should be deterministic, documented, and safe to run against sanitized data. Pin dependencies, include sample input, and emit machine-readable results. Never commit API keys or private session exports.
